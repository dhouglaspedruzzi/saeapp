<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    tools:context="com.app.sae.sae.HorarioActivity">

    <TextView
        android:id="@+id/espacoTitulo"
        android:gravity="center"
        android:textSize="20sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        />

    <TextView
        android:id="@+id/dataTitulo"
        android:gravity="center"
        android:textSize="20sp"
        android:layout_below="@+id/espacoTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="selecionarData"
        android:layout_centerHorizontal="true"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/horarios_rv"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_below="@+id/dataTitulo"
        android:layout_above="@+id/novaLeituraBtn"
        android:layout_margin="8dp"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/novaLeituraBtn"
        android:text="@string/btn_nova_leitura"
        android:layout_gravity="center"
        android:textColor="@color/colorDefault"
        android:background="@color/colorUno"
        android:padding="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="novaLeitura"/>

</RelativeLayout>
